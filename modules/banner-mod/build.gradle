archivesBaseName = project.archives_base_name + "-mod"
version = getSubprojectVersion(project)

moduleDependencies(project, [
        "banner-server",
        "banner-bootstrap",
        "banner-api",
        "banner-bukkit-manager",
        "banner-plugin",
        "banner-stackdeobf",
        "banner-transitive-access-wideners",
])

processResources {
    duplicatesStrategy = DuplicatesStrategy.WARN
}